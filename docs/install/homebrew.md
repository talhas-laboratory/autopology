# Homebrew Install

AuTopology Homebrew installs standalone binaries, so users do not need Node.js at runtime.

## Add Tap

```bash
brew tap talhas-laboratory/homebrew-tap
```

## Install

```bash
brew install autopology
```

## Verify

```bash
autopology --help
autopology doctor --repo /path/to/repo
```
